pipeline {
    agent any
	options { checkoutToSubdirectory('foo') }
    stages {
        stage('Example') {
            steps {                
                echo 'Hello World 1'
            }
        }
    }
}
