pipeline {
    agent any
  options { 
	skipDefaultCheckout() 
	}
    stages {
        stage('Example') {
            steps {
                sleep(time: 1, unit: 'SECONDS') 
                echo 'Hello World 1'
            }
        }
    }
}
