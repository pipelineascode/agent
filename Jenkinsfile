pipeline {
    agent any
	options { checkoutToSubdirectory('z:\foo') }
    stages {
        stage('Example') {
            steps {                
                echo 'Hello World 1'
            }
        }
    }
}
